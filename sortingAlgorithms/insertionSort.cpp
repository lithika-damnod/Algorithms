#include <iostream> 
#include <vector> 
#include <string> 
using namespace std; 

//insertion sort <int>
vector<int> insertionSort(vector<int> arr){
	for(int i=1; i<arr.size(); i++){
		int index = i; 
		if(arr[i-1] > arr[i]){
			while() //something to test pull request - git - github
		}
	}
}

int main(){
	vector<int>para = {1, 4, 2, 10, 2, 3, 5};
	insertionSort(para); 
	return 0; 
}
